import AuthProvider from "@/components/AuthProvider/AuthProvider";
import { useAuth, useTheme } from "@/components/AuthProvider/hook";
import { Theme, AuthContext } from "@/components/AuthProvider/types";

export { AuthProvider, useAuth, useTheme };
export type { Theme, AuthContext };
